export default defaultSpicrOptions;
declare namespace defaultSpicrOptions {
    const delay: number;
    const duration: number;
    const easing: string;
    const interval: number;
    const touch: boolean;
    const pause: string;
}
